# Bloqueia todo o acesso HTTP a esta directoria.
# Copiar para: public_html/.private/.htaccess
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
